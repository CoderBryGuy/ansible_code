---
- name: Get MAC info
  hosts: gns3-core:gns3-access
  gather_facts: false

  tasks:
    - name: show mac address table
      raw: "show mac address-table"
      register: print_output

    - name: print MAC output
      debug:
        var: print_output.stdout_lines
