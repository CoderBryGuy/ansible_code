---
- name: Get ARP info
  hosts: gns3-core:gns3-access
  gather_facts: false

  tasks:
    - name: show ARP
      raw: "show arp"
      register: print_output

    - name: print ARP output
      debug:
        var: print_output.stdout_lines
